#ifndef CAGREGATOR_HPP
#define CAGREGATOR_HPP

#include <map>
#include <random>
#include "Algo/ProcessingTypes.h"
#include "Data/Project.hpp"

/*
class CAgregator
{


public:
    CAgregator(Data::CProject & );




public:

protected:
    struct Cache {
        AverageRange m_saturation;
        AverageRange m_value;
    };
    struct Randomiser
    {
        std::bernoulli_distribution m_noise_generator;
        std::bernoulli_distribution m_use_generator;
        std::uniform_int_distribution<size_t> m_chooser;
        std::mt19937 m_engine;
    };

protected:
    std::map<const QString *, Cache> m_mapped_chache;
    std::map<const Data::CLayer *, Randomiser> m_mapped_additionals;
    Data::CProject & m_project;


};
*/

#endif // CAGREGATOR_HPP
